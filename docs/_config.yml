# Settings for the GitHub Pages site built from this folder.
# The title names the site in its header and in the browser tab. Without it,
# GitHub Pages falls back to the repository's name, "ip".
title: Nori
